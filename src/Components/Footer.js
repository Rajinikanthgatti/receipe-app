import React, {useEffect} from 'react'

export const Footer = ({resp}) => {
    return (
        <div className="footer">
            <div>
            <button className="search-button-prev">Prev</button>
            <button className="search-button-next">Next</button>
            </div>
        </div>
    )
}
